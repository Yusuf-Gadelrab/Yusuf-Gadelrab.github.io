"""Guide content: AI engineering, part two — embeddings, structured output, local MoE models,
prompt caching, and static sites answer engines can read. Data only; rendering lives in engine.py."""

NAV_DEV = [("/", "Work"), ("/guides.html", "Guides"), ("/stack.html", "Stack"), ("/apps.html", "Apps")]

GUIDES = [

{
 "slug": "embedding-model-rag",
 "title": "Choosing an Embedding Model for RAG",
 "desc": "How to pick an embedding model for RAG: dimensions vs cost, local vs API, chunking interactions, recall@k evaluation, and when a reranker wins.",
 "og_title": "Choosing an Embedding Model for RAG",
 "og_desc": "Dimensions, storage cost, local vs API, recall@k evaluation, rerankers — and the honest answer that chunking usually matters more than the model.",
 "headline": "Choosing an Embedding Model for RAG: Dimensions, Cost, and What Actually Moves Recall",
 "crumb": "Embedding Models",
 "about": ["Retrieval-augmented generation", "Vector search", "Embedding models", "Information retrieval"],
 "eyebrow": "Guide · AI engineering",
 "h1": "Picking an embedding model, and why it matters less than you think",
 "lead": "Every RAG tutorial spends ten minutes on which embedding model to use and thirty seconds on how to split the documents. That is exactly backwards. Here is what the model choice actually buys you, how to measure it instead of guessing, and the three decisions that move recall far more than swapping one model for another.",
 "level": "Intermediate",
 "nav": NAV_DEV,
 "body": [
  ("h2", "The question, and the better question"),
  ("p", "The question that gets asked is \"which embedding model should I use?\" It has a boring answer: pick a well-regarded general-purpose model at 768 or 1024 dimensions, ship it, and move on. The question worth asking is \"why is my retrieval missing the obviously relevant paragraph?\" — and in most broken pipelines the answer has nothing to do with the model. It is a chunk that split a table away from its header, a query phrased as a question against chunks written as statements, or a top-k of 3 when the answer needed 8."),
  ("p", "This guide covers the model choice properly, because it does matter at the margins, and then covers the things that matter more. If you are new to the overall pipeline, <a href=\"/guides/rag-explained.html\">the RAG explainer</a> is the prerequisite; this one assumes you already know what a vector store is and are now trying to make one work well."),
  ("h2", "What an embedding actually is"),
  ("p", "An embedding model is a neural network that reads text and outputs a fixed-length list of floating-point numbers — a vector. A 1024-dimension model always returns 1024 numbers, whether the input was one word or four hundred. The model was trained so that texts humans would consider related land close together in that space, and unrelated texts land far apart. Nothing about the vector is human-readable. Dimension 417 does not mean \"legal document.\" The only thing you can do with a vector is compare it to another vector."),
  ("p", "The comparison is almost always cosine similarity: the cosine of the angle between two vectors, which ignores their lengths and measures only direction. It runs from -1 (opposite) through 0 (unrelated) to 1 (identical direction). Most modern embedding models output unit-length vectors, which makes cosine similarity and the dot product the same operation, which is why vector databases can compare millions of vectors per second."),
  ("formula", "cos(A, B) = (A · B) / (||A|| × ||B||)\n\nwith unit-length vectors this collapses to just the dot product:\ncos(A, B) = A · B = a1×b1 + a2×b2 + ... + an×bn"),
  ("p", "The critical consequence: <strong>similar does not mean relevant, and it definitely does not mean correct.</strong> An embedding model trained on general web text will happily rank \"the refund policy is 30 days\" and \"the refund policy is 90 days\" as near-identical, because they are near-identical as language. Vector search finds text that looks like the query. Everything downstream has to survive that limitation."),
  ("h2", "The four numbers on a model card that matter"),
  ("table", (["Spec", "What it controls", "How much to care"],
             [["Dimensions", "Vector size — storage, index memory, and comparison speed", "A lot, for cost. Very little, for quality above ~768."],
              ["Max sequence length", "How much text one embedding can represent before silent truncation", "A lot. This is the one that quietly breaks pipelines."],
              ["Benchmark score (MTEB and similar)", "Average retrieval quality across a fixed set of public tasks", "A little. Rankings compress into a few points near the top."],
              ["Parameters / latency", "How long it takes to embed a document set and each incoming query", "A lot at index time, a little at query time."]])),
  ("h3", "Dimensions: a storage decision, not a quality decision"),
  ("p", "Dimensions are the number everyone fixates on, and they are mostly a cost lever. Storage is exactly linear and easy to compute: dimensions × 4 bytes for float32, times the number of chunks."),
  ("formula", "bytes = n_chunks × dimensions × 4        (float32)\n\n 10,000 chunks ×  768 dims × 4 =  30.7 MB\n 10,000 chunks × 1536 dims × 4 =  61.4 MB\n 10,000 chunks × 3072 dims × 4 = 122.9 MB\n100,000 chunks × 1024 dims × 4 = 409.6 MB"),
  ("p", "Then add index overhead. An HNSW graph — the default in most vector databases — typically costs another 1.5× to 2× on top of the raw vectors, and it wants to live in RAM. At 10,000 chunks none of this matters on any machine built this decade. At 5 million chunks, the difference between 768 and 3072 dimensions is the difference between a small instance and a large one, every month, forever."),
  ("p", "The quality return on dimensions flattens fast. Going from 384 to 768 is usually a real improvement. From 768 to 1024, small. From 1536 to 3072, often within measurement noise on a domain-specific corpus. Some newer models support Matryoshka representation learning, where you can truncate the vector to a shorter prefix and keep most of the quality — that is a genuinely useful lever if you have it, because it lets you make the storage decision after you have measured, instead of before."),
  ("h3", "Max sequence length: the silent truncation trap"),
  ("p", "Every embedding model has a maximum input length, typically 512 tokens for older models and 8,192 for newer ones. Feed it more and most implementations <strong>truncate without warning</strong>. Your 1,200-token chunk gets embedded as its first 512 tokens, the rest is discarded, and retrieval quietly fails for any query whose answer lived in the back half. There is no error, no log line, and no way to notice except by measuring recall."),
  ("p", "This is where model choice and chunking interact, and it is the one interaction you must get right. Chunk to comfortably under the model's limit, and verify by tokenizing a sample of your longest chunks with the model's own tokenizer rather than estimating with a characters-per-token rule of thumb — that rule is wrong by 30% or more on code, tables, and non-English text."),
  ("h3", "Benchmark scores: a filter, not a ranking"),
  ("p", "Public retrieval leaderboards are useful for excluding bad models and nearly useless for choosing between good ones. The top twenty models sit within a few points of each other on an average over tasks that are probably nothing like yours, and models get tuned against those benchmarks, which inflates the top of the board relative to real-world behavior. Use the leaderboard to build a shortlist of three. Use your own data to pick from the shortlist."),
  ("h2", "Local versus API embeddings"),
  ("p", "The tradeoff is different from the one for generation models, because embedding models are small. A strong general-purpose embedder is often a few hundred million parameters, which runs comfortably on a laptop CPU and very fast on any GPU. You do not need a 30B model to embed a paragraph, which is why local embeddings are much more often the right call than local generation."),
  ("table", (["", "Local (Ollama, sentence-transformers)", "Hosted API"],
             [["Cost", "Electricity. Effectively free at any volume you will hit as one person.", "Per-token, and re-indexing a large corpus means paying again."],
              ["Privacy", "Documents never leave the machine. Often the whole reason.", "Every chunk is sent to a third party at index time."],
              ["Speed", "Fast enough. Batch embedding a 10k-chunk corpus is minutes, not hours.", "Network round-trip per batch; rate limits at scale."],
              ["Quality", "Very close to hosted for general English text.", "Usually a small edge, larger on multilingual and specialized domains."],
              ["Failure mode", "You maintain the runtime and the model version.", "A model deprecation forces a full re-index on someone else's schedule."]])),
  ("p", "Pull and call a local embedder in two commands. Note that this is the HTTP API, not the interactive CLI — for anything programmatic, always talk to the API, which returns structured JSON instead of a stream of terminal formatting you would have to parse:"),
  ("formula", "ollama pull nomic-embed-text\n\ncurl -s http://localhost:11434/api/embed \\\n  -d '{\"model\":\"nomic-embed-text\",\"input\":[\"first chunk\",\"second chunk\"]}' \\\n  | python3 -c \"import sys,json; d=json.load(sys.stdin); print(len(d['embeddings']), len(d['embeddings'][0]))\"\n\n# -> 2 768   (two vectors, 768 dimensions each)"),
  ("callout", "One thing that surprises people: Anthropic does not ship an embedding model. Claude is a generation model, and a RAG pipeline built around Claude uses someone else's embedder — local or hosted — for the retrieval half and Claude only for the answer. The two halves are genuinely independent, which is good news: you can change either one without touching the other, as long as you re-index when you change the embedder."),
  ("h2", "Chunking is the variable you actually control"),
  ("p", "Here is the honest hierarchy, in descending order of how much each one moves retrieval quality on a typical corpus: how you split the documents, how many results you retrieve, whether you rerank, and only then which embedding model you used. Teams routinely spend a week A/B-testing embedders for a two-point recall difference while running a fixed 512-character split that severs every table from its caption."),
  ("p", "What actually works, in rough order of impact:"),
  ("ul", [
   "<strong>Split on structure, not on character count.</strong> Headings, sections, list items, function definitions. A chunk should be a thing, not a fixed-width window that happened to land there.",
   "<strong>Keep the chunk self-contained.</strong> If it says \"this applies to the above,\" retrieval will surface it alone and the model will have no idea what \"above\" was. Prepend the document title and section heading to every chunk — it costs a few tokens and it is the single cheapest quality win available.",
   "<strong>Overlap by 10–15%.</strong> Enough that a sentence spanning a boundary survives in at least one chunk. More than 20% and you are paying storage to store the same text repeatedly.",
   "<strong>Never split a table, a code block, or a numbered list.</strong> These are the chunks most likely to hold the actual answer and the most destroyed by a naive split.",
   "<strong>Aim for 200–500 tokens.</strong> Short enough that the embedding is about one thing, long enough to carry context. Well under any modern model's sequence limit.",
   "<strong>Store the parent document ID and offsets.</strong> Retrieve the chunk, then optionally expand to its neighbors before handing text to the generation model. Retrieval granularity and context granularity do not have to match.",
  ]),
  ("h2", "How to evaluate: recall@k, on fifty of your own questions"),
  ("p", "Retrieval is measurable, and measuring it takes an afternoon. Build a golden set: fifty real questions someone would actually ask your corpus, each labeled with the chunk or document that contains the answer. Fifty is not statistically luxurious, but it will reliably show you a ten-point difference, which is the size of difference worth acting on."),
  ("p", "Then compute recall@k — the fraction of questions where a correct chunk appears anywhere in the top k results. This is the metric that matters, because the generation model does not care whether the right chunk ranked first or fourth, only whether it was in the window at all."),
  ("formula", "recall@k = (queries where a relevant chunk is in the top k) / (total queries)\n\nMRR = mean over queries of 1 / (rank of the first relevant chunk)\n\n# recall@k answers: did we retrieve it at all?\n# MRR answers:      how high did it rank?\n# Optimize recall@k first. Then use a reranker to fix ordering."),
  ("p", "A tiny harness is enough. Run the golden set against configuration A, run it against configuration B, print both numbers, and change exactly one thing at a time:"),
  ("formula", "def recall_at_k(store, golden, k=10):\n    hits = 0\n    for question, expected_id in golden:\n        results = store.search(question, k=k)\n        if any(r.chunk_id == expected_id for r in results):\n            hits += 1\n    return hits / len(golden)\n\nfor k in (1, 3, 5, 10, 20):\n    print(k, round(recall_at_k(store, GOLDEN, k), 3))"),
  ("p", "The shape of that curve tells you what to fix. If recall@20 is high but recall@3 is low, retrieval is finding the right chunk and ranking it badly — that is a reranker problem. If recall@20 is <em>also</em> low, the right chunk is not being found at all, which is a chunking or an embedding problem, and chunking is the far more likely culprit. Diagnosing which of the two you have is the whole point of running the sweep before touching anything."),
  ("h2", "When a reranker beats a better embedder"),
  ("p", "An embedding model compares a query vector to a chunk vector — the two were encoded separately and never saw each other. That is what makes vector search fast: you precompute every chunk once. A cross-encoder reranker instead reads the query and the chunk <em>together</em> and scores the pair directly, which is dramatically more accurate and far too slow to run against a whole corpus."),
  ("p", "So you use both. Retrieve wide and cheap with vectors, then rerank narrow and expensive with a cross-encoder."),
  ("formula", "query -> vector search, k=50   (fast, approximate, high recall)\n      -> cross-encoder rerank -> keep top 8   (slow, accurate, high precision)\n      -> generation model\n\n50 cross-encoder scorings per query: tens of milliseconds.\n5,000,000 cross-encoder scorings per query: not a product."),
  ("p", "In practice a mediocre embedder plus a reranker beats an excellent embedder alone on nearly every corpus, because the reranker is doing a fundamentally harder and more informative computation. If you have a fixed budget of engineering time, adding a reranker is a better use of it than upgrading an embedder. The catch is latency: the rerank step adds real milliseconds to every query, so it belongs in a pipeline where the user is already waiting on a generated answer, not in an autocomplete."),
  ("h2", "The migration cost nobody budgets for"),
  ("p", "Vectors from different models are not comparable. Not \"slightly less accurate\" — meaningless. Different dimensionality, different geometry, no shared reference frame. Changing your embedding model means re-embedding every chunk in the corpus and rebuilding the index, with no partial migration and no fallback."),
  ("warn", "This is why an API embedding model deprecation is a genuine operational event and not a version bump. Keep the raw source text alongside your vectors, always, so a re-index is a batch job and not an archaeology project. If you only stored the vectors, you cannot migrate — you have to re-crawl the source, and the source may have changed.")
  ,
  ("h2", "A decision table"),
  ("table", (["Situation", "What to do"],
             [["First RAG build, under 50k chunks", "Local embedder at 768 dimensions, structural chunking, top-k 10. Ship it, then measure."],
              ["Documents are sensitive or under NDA", "Local, non-negotiable. The privacy argument is stronger than any quality argument at this scale."],
              ["Recall@20 is high, recall@3 is low", "Add a reranker. Do not touch the embedder."],
              ["Recall@20 is also low", "Fix chunking. Check for silent truncation first, then check whether chunks are self-contained."],
              ["Multilingual or heavily domain-specific corpus", "This is where a better hosted embedder genuinely earns its cost. Measure it against your golden set before committing."],
              ["Millions of chunks and rising infra cost", "Drop dimensions or quantize the vectors to int8. Measure the recall cost — it is usually smaller than expected."],
              ["Exact identifiers, SKUs, error codes in queries", "Add keyword search alongside vectors (hybrid retrieval). Embeddings are bad at exact-match by construction."]])),
  ("h2", "What I actually run"),
  ("p", "My own retrieval work runs local end to end: a local embedding model over documents that never leave the machine, and <a href=\"/guides/local-llm-ollama.html\">a local generation model via Ollama</a> — currently a 30B mixture-of-experts model at roughly 40k context — for the answering half. The reason is not cost, it is that the corpora involved are personal notes and unpublished research, and \"this never touched a third-party API\" is a property worth more than two points of benchmark score."),
  ("p", "The one place I stopped optimizing the model and started optimizing the pipeline was when I built the retrieval side of an equity research platform during an IBM SkillsBuild program: an NLP scoring system reading 50+ live market sources a day, hitting 78% directional accuracy and cutting manual research time by about 60%. Nearly all of the quality came from how the source documents were segmented and filtered before they were ever embedded. The embedder was the least interesting decision in the system."),
 ],
 "tools": [("/guides/rag-explained.html", "RAG explained", "the pipeline this guide plugs into — chunking, retrieval, reranking, and where it breaks."),
           ("/guides/local-llm-ollama.html", "Local LLM with Ollama", "running the generation half on your own machine."),
           ("/guides/moe-model-macbook.html", "30B MoE on a MacBook", "what it costs in RAM and tokens/sec to keep the whole pipeline local."),
           ("/stack.html", "My stack", "the tools behind the retrieval and automation work.")],
 "faqs": [
  ("What is an embedding in RAG, in plain terms?", "An embedding is a fixed-length list of numbers that a model produces from a piece of text, arranged so that texts with similar meaning produce vectors pointing in similar directions. Retrieval works by embedding the user's question, comparing that vector against every stored chunk vector with cosine similarity, and returning the closest ones. Nothing about the numbers is human-readable; the only operation available is comparison."),
  ("Do more embedding dimensions mean better retrieval?", "Barely, above roughly 768 dimensions, and the storage cost is exactly linear. Going from 384 to 768 usually helps; going from 1536 to 3072 is often within measurement noise on a domain-specific corpus while doubling your index size and memory. Treat dimensions as a cost decision you make after measuring recall, not a quality decision you make before."),
  ("Should I use a local embedding model or a hosted API?", "Local is the right default for most projects. Embedding models are small enough to run fast on a laptop, so you get free re-indexing, no rate limits, and documents that never leave your machine. Hosted APIs earn their cost mainly on multilingual corpora, heavily specialized domains, and cases where you genuinely cannot run infrastructure."),
  ("How do I know if my embedding model is the problem?", "Build a golden set of about fifty real questions with known correct chunks, then measure recall@k at k of 1, 3, 5, 10 and 20. If recall@20 is high but recall@3 is low, ranking is the problem and a reranker fixes it. If recall@20 is also low, the right chunk is never being retrieved, which is almost always a chunking problem rather than a model problem."),
  ("When does a reranker beat switching to a better embedding model?", "Almost always, when your recall@20 is already decent. A cross-encoder reranker reads the query and the candidate chunk together, which is a fundamentally more accurate computation than comparing two independently-encoded vectors. Retrieve 50 candidates with vector search, rerank them down to 8, and you typically beat any embedder upgrade for less engineering effort."),
  ("Can I mix embeddings from two different models in one index?", "No. Vectors from different models live in different spaces with different geometry and often different dimensionality, so comparing them produces meaningless similarity scores. Changing your embedding model requires re-embedding the entire corpus and rebuilding the index, which is why you should always keep the raw source text alongside the vectors."),
  ("What is the most common silent failure in an embedding pipeline?", "Truncation. Every embedding model has a maximum input length, and most implementations silently discard anything beyond it rather than raising an error, so a chunk longer than the limit gets embedded from only its first section. Verify by tokenizing your longest chunks with the model's own tokenizer, since character-count estimates are wrong by 30% or more on code, tables, and non-English text."),
 ],
 "faq_cards": [
  ("What is an embedding?", "A fixed-length vector a model produces from text, arranged so similar meanings point in similar directions. Retrieval compares the question's vector to every chunk's vector by cosine similarity."),
  ("Do more dimensions help?", "Barely above ~768, and storage cost is exactly linear. 384 to 768 helps; 1536 to 3072 is often noise while doubling index size. It is a cost decision, not a quality one."),
  ("Local or hosted embeddings?", "Local by default — embedders are small, so you get free re-indexing and documents that never leave the machine. Hosted earns its cost on multilingual and specialized corpora."),
  ("Is the model my problem?", "Measure recall@k on 50 real questions. High recall@20 + low recall@3 = ranking problem, add a reranker. Low recall@20 too = chunking problem, not a model problem."),
  ("Reranker or better embedder?", "Reranker, almost always. A cross-encoder reads query and chunk together, which beats comparing two separately-encoded vectors. Retrieve 50, rerank to 8."),
  ("Can I mix two models' vectors?", "No. Different models produce different geometry and often different dimensionality, so the similarity scores are meaningless. Switching embedders means re-embedding everything."),
  ("Most common silent failure?", "Truncation. Models cap input length and most libraries discard the overflow without an error, so long chunks get embedded from their first section only. Tokenize to verify."),
 ],
},

{
 "slug": "structured-llm-output",
 "title": "Structured Output From an LLM Without Regret",
 "desc": "JSON mode vs tool calling vs constrained decoding vs schema validation with retry, why parsing free text fails, and idempotency for partial LLM output.",
 "og_title": "Structured Output From an LLM Without Regret",
 "og_desc": "Four mechanisms ranked, a worked JSON Schema and retry loop, and the two failure modes — truncation and duplicated side effects — nobody plans for.",
 "headline": "Structured Output From an LLM Without Regret: JSON Mode, Tool Calling, Constrained Decoding, and the Retry Loop You Still Need",
 "crumb": "Structured Output",
 "about": ["Large language models", "JSON Schema", "API design", "Software reliability"],
 "eyebrow": "Guide · AI engineering",
 "h1": "Getting JSON out of a language model, reliably",
 "lead": "Every LLM feature that touches a database eventually needs the model to return structured data instead of prose. There are four mechanisms for that, they are not equivalent, and three of them still need a validation layer you have to write yourself. Here is the ladder, the failure modes at each rung, and the two problems — truncation and duplicated side effects — that no amount of schema enforcement solves.",
 "level": "Intermediate",
 "nav": NAV_DEV,
 "body": [
  ("h2", "Why regex-ing the response fails"),
  ("p", "The first version of every extraction feature asks the model to \"return JSON\" and then parses whatever comes back. It works in testing, because in testing you send five inputs and they all look alike. It fails in production, and it fails in a specific, tediously predictable set of ways."),
  ("ul", [
   "A markdown fence wrapped around the JSON, because the model is trained on chat formatting.",
   "A helpful preamble: <code>Here is the extracted data:</code> followed by the object.",
   "A trailing comma, or a single quote, or an unescaped newline inside a string.",
   "A field you never defined, invented because the input mentioned it and the model wanted to be useful.",
   "A field you did define, missing entirely, because the input did not mention it and the model decided omitting was more honest than guessing.",
   "The value <code>\"N/A\"</code> in an integer field. Or <code>\"unknown\"</code>. Or <code>null</code> in a field you did not mark nullable.",
   "An apology instead of an object, because something about the input triggered a refusal.",
  ]),
  ("p", "Each of these is individually fixable with a regex, and the regex pile is exactly how you end up with a 200-line parser that is itself the least reliable component in the system. The fix is not a better parser. The fix is to stop letting the model emit invalid output in the first place, and then to validate what it does emit anyway."),
  ("h2", "The four mechanisms, from worst to best"),
  ("table", (["Mechanism", "What it guarantees", "What it does not"],
             [["Prompt and parse", "Nothing.", "Everything. Do not build on this."],
              ["JSON mode", "The output is syntactically valid JSON.", "That it matches your schema, has your fields, or uses your types."],
              ["Tool / function calling", "The model emits a named call with an arguments object shaped like your schema.", "Deep validation of nested constraints, unless the provider offers a strict mode."],
              ["Constrained decoding", "Token-by-token, the model is only permitted to emit tokens that keep the output schema-valid. Invalid output is unrepresentable.", "That the content is correct. A schema-perfect object can still be wrong."]])),
  ("h3", "JSON mode"),
  ("p", "JSON mode is a flag that says \"the response body must parse as JSON.\" It solves the markdown fence, the preamble, and the trailing comma. It does not solve the schema — you can get back valid JSON with none of your fields, or with your fields carrying types you never asked for. It is a real improvement over parsing prose and it is not a solution."),
  ("h3", "Tool calling"),
  ("p", "Tool calling is the mechanism most people should reach for, because it was designed for exactly this. You describe a function with a JSON Schema for its parameters, the model returns a structured call to that function, and the provider handles the shaping. The subtle win is that it aligns with the model's training: models are heavily trained to emit well-formed tool calls, so you get better adherence than from a prompt asking for the same shape."),
  ("p", "Many providers now offer a strict mode on top of this — a flag on the tool definition that makes the provider actually enforce the schema rather than treat it as a strong suggestion. Where that exists, use it. It typically requires <code>additionalProperties: false</code> and an explicit <code>required</code> array, which is a reasonable price for the guarantee."),
  ("h3", "Constrained decoding"),
  ("p", "Constrained decoding is the strongest mechanism and the one most people do not know they have. At each generation step, the model produces a probability distribution over the entire vocabulary; a grammar engine masks out every token that would make the partial output invalid against your schema, and sampling happens only over what remains. The model is not asked nicely to produce valid JSON — it is structurally incapable of producing anything else."),
  ("p", "This is available locally for free. Ollama accepts a JSON Schema in the <code>format</code> field and enforces it during generation; llama.cpp exposes the same capability as GBNF grammars; libraries like Outlines and XGrammar implement it over other runtimes. If you are running <a href=\"/guides/local-llm-ollama.html\">a model locally</a>, this is the single highest-leverage feature you are probably not using:"),
  ("formula", "curl -s http://localhost:11434/api/chat -d '{\n  \"model\": \"qwen3\",\n  \"stream\": false,\n  \"messages\": [{\"role\": \"user\", \"content\": \"Extract the load details: ...\"}],\n  \"format\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"origin_city\":  {\"type\": \"string\"},\n      \"rate_usd\":     {\"type\": \"number\"},\n      \"equipment\":    {\"type\": \"string\", \"enum\": [\"dry_van\",\"reefer\",\"flatbed\",\"unknown\"]}\n    },\n    \"required\": [\"origin_city\", \"rate_usd\", \"equipment\"]\n  }\n}'"),
  ("callout", "Constrained decoding guarantees the shape and says nothing about the content. A model that does not know the rate will still emit a number, because the grammar requires one. Schema enforcement converts \"unparseable garbage\" into \"parseable garbage\" — which is a large improvement, since parseable garbage can be validated, logged, and caught, while unparseable garbage just throws.")
  ,
  ("h2", "You still need validation with retry"),
  ("p", "Whichever mechanism you use, a validation layer belongs in front of your business logic. It is the only place that can enforce the constraints schemas cannot express — that a date is in the past, that a total equals the sum of the line items, that an ID exists in your database — and it is the layer that gives the model a second chance with a useful error message instead of dying."),
  ("p", "The pattern is short. Validate; on failure, feed the validation error back as a new turn and try once more; cap the attempts and fail loudly."),
  ("formula", "from pydantic import BaseModel, ValidationError, Field\nfrom typing import Literal\n\nclass Load(BaseModel):\n    origin_city: str = Field(min_length=2)\n    rate_usd: float = Field(gt=0)\n    equipment: Literal[\"dry_van\", \"reefer\", \"flatbed\", \"unknown\"]\n    pickup_date: str  # ISO 8601\n\ndef extract(text: str, max_attempts: int = 3) -> Load:\n    messages = [{\"role\": \"user\", \"content\": PROMPT + text}]\n    for attempt in range(max_attempts):\n        raw, finish = call_model(messages, schema=Load.model_json_schema())\n\n        if finish == \"max_tokens\":            # truncation, not a parse bug\n            raise OutputTruncated(attempt)    # retrying identically will not help\n\n        try:\n            return Load.model_validate_json(raw)\n        except ValidationError as e:\n            messages += [\n                {\"role\": \"assistant\", \"content\": raw},\n                {\"role\": \"user\",\n                 \"content\": f\"That failed validation:\\n{e}\\nReturn corrected JSON only.\"},\n            ]\n    raise ExtractionFailed(f\"{max_attempts} attempts exhausted\")"),
  ("p", "Two details in there are load-bearing. First, the validation error text goes back to the model verbatim — a good validator's error message names the field and the constraint, which is exactly the information needed to fix it, and models are strikingly good at acting on it. Second, truncation is detected separately and never retried the same way, for reasons in the next section."),
  ("h2", "Truncation is not a parse error"),
  ("p", "When a response hits the output token limit, generation stops mid-token. With constrained decoding you get a syntactically valid prefix of an invalid document; without it you get a string ending in <code>\"origin_city\": \"Sto</code>. Both surface at your parser as a parse failure, and treating them as one is the bug: no amount of retrying an identical request fixes a limit that is too low."),
  ("p", "Every major API returns a stop reason or finish reason on the response. Check it <em>before</em> you check the parse. If it indicates the token limit was hit, that is a configuration failure — raise the limit, shrink the requested output, or split the extraction into smaller pieces. Retrying the same call gets the same truncation and burns money doing it."),
  ("warn", "The related trap is streaming. If you stream a structured response and parse incrementally, every intermediate state is an incomplete document. Partial-JSON parsers exist and are genuinely useful for progressive UI, but nothing downstream of a partial parse may perform a side effect — you are looking at a document that is still being written and whose remaining fields may contradict what you already read.")
  ,
  ("h2", "Idempotency: the retry loop's real cost"),
  ("p", "Here is the failure that reaches production: extraction succeeds, the write succeeds, the response times out on the way back, the retry logic fires, and the same record is written twice. The model behaved perfectly. The retry loop did the damage."),
  ("p", "The rule is simple and easy to violate: <strong>the retry loop must never contain a side effect.</strong> Structure the code so the model call and validation happen inside the loop and the write happens strictly after it returns."),
  ("formula", "# wrong — a validation retry can re-run the insert\nfor attempt in range(3):\n    data = call_model(...)\n    db.insert(data)          # side effect INSIDE the loop\n\n# right — retries are pure; the write happens once, keyed\ndata = extract(text)          # loop is contained here, no side effects\ndb.upsert(data, key=idempotency_key(source_document_id))"),
  ("p", "Then make the write itself idempotent with a key you derive, not one the model invents. A hash of the source document plus the extraction version is ideal: deterministic, collision-resistant, and stable across retries. Never let the model generate the idempotency key — it will generate a different one on the retry, which is precisely the case the key exists to prevent."),
  ("h2", "Schema design that reduces retries"),
  ("p", "Most validation failures are avoidable by writing a better schema, which is cheaper than handling them at runtime."),
  ("ul", [
   "<strong>Give every enum an escape hatch.</strong> Add <code>\"unknown\"</code> or <code>\"other\"</code> as a permitted value. Without one, a model facing an input that fits nothing on your list will pick the closest wrong option, and you will never know it guessed.",
   "<strong>Put a reasoning field first, and the answer after.</strong> Schemas are filled in order, so a <code>reasoning</code> string before <code>classification</code> lets the model work through the problem in its own output before committing. It costs tokens and measurably improves accuracy on judgment calls.",
   "<strong>Write descriptions for every field.</strong> The schema is part of the prompt. <code>\"rate_usd\": total linehaul rate in USD, excluding fuel surcharge</code> prevents a category of error that no amount of type enforcement catches.",
   "<strong>Prefer flat over deeply nested.</strong> Every level of nesting is another place for a structure mistake. Two calls with flat schemas beat one call with a four-level tree.",
   "<strong>Mark optional fields optional.</strong> If you require a field the source often lacks, you are asking the model to fabricate. It will.",
   "<strong>Set <code>additionalProperties: false</code>.</strong> Both a real constraint and, in strict modes, a prerequisite for enforcement.",
  ]),
  ("h2", "What to log"),
  ("p", "Structured output failures are close to impossible to debug after the fact without the raw string. Log, on every failure: the raw model output before parsing, the stop reason, the validation error, the attempt number, and the schema version. That last one matters more than it sounds — schemas drift, and \"this used to work\" is usually \"the schema changed and nothing recorded which version produced this row.\""),
  ("p", "Track your retry rate as an actual metric. A stable 2% is the system working. A rate climbing over weeks means your inputs are drifting away from what your prompt and schema were built for, and the retry loop is quietly absorbing it until the day it cannot. This is the same discipline as the automated quality gates I use for <a href=\"/guides/ai-automation-small-business.html\">generated outreach drafts</a>: the model produces, a deterministic checker validates, and nothing ships that fails the check."),
  ("h2", "A short decision list"),
  ("table", (["Situation", "Use"],
             [["Local model, any structured task", "Constrained decoding — Ollama's <code>format</code> field with a JSON Schema. Free, strongest guarantee available."],
              ["Hosted API, extraction or classification", "Tool calling with strict mode if the provider has it, plus a validation layer."],
              ["Hosted API, no strict mode", "Tool calling plus validation with a capped retry loop."],
              ["Output is long (large arrays, many records)", "Split into batched calls. Long structured outputs truncate, and truncation is the failure retries cannot fix."],
              ["Any write to a database or external service", "Idempotency key derived from the source, upsert outside the retry loop, always."],
              ["Free-text summary with no downstream consumer", "Do not force a schema. Structured output is for machine consumers, and JSON overhead on prose buys nothing."]])),
  ("p", "The through-line is that no single mechanism is sufficient. Constrained decoding gives you shape, validation gives you semantics, idempotency gives you safety under retry, and logging gives you the ability to fix it when the inputs change. Skip any one of them and the gap shows up eventually — usually as a duplicated row someone finds three weeks later. If you are building this into a real product, the same care belongs on the surrounding infrastructure; <a href=\"/guides/student-founder-security-checklist.html\">the security checklist</a> covers the parts that bite next."),
 ],
 "tools": [("/guides/local-llm-ollama.html", "Local LLM with Ollama", "where constrained decoding via the format field is free and strongest."),
           ("/guides/rag-explained.html", "RAG explained", "the other half of most LLM features that touch real data."),
           ("/guides/student-founder-security-checklist.html", "Security checklist", "the surrounding infrastructure a data-writing LLM feature needs."),
           ("/projects.html", "Projects", "extraction and automation pipelines built on these patterns.")],
 "faqs": [
  ("What is the difference between JSON mode and tool calling?", "JSON mode only guarantees that the response parses as valid JSON — it says nothing about which fields are present or what types they hold. Tool calling has you define a JSON Schema for a function's parameters and the model returns a structured call shaped to that schema, which is far closer to what you actually want. Tool calling is the better default because models are heavily trained on emitting well-formed tool calls."),
  ("What is constrained decoding and why is it stronger?", "Constrained decoding masks the model's token choices at every generation step so that only tokens keeping the output valid against your schema are sampleable. The model is structurally unable to emit invalid output, rather than being asked to avoid it. Ollama exposes this through the format field, llama.cpp through GBNF grammars, and libraries like Outlines and XGrammar over other runtimes."),
  ("Do I still need schema validation if the provider enforces the schema?", "Yes. Schema enforcement guarantees shape, not semantics — it cannot check that a date is in the past, that a total equals the sum of its line items, or that an ID exists in your database. A validation layer also gives you a place to catch the failure and feed a specific error message back to the model for one corrective attempt."),
  ("How should I handle a truncated LLM response?", "Check the response's stop or finish reason before you check the parse. If generation hit the token limit, that is a configuration failure and retrying the identical request will truncate identically — raise the limit, request less output, or split the task into batches. Treating truncation as a parse error is the most common way to burn money on a loop that cannot succeed."),
  ("How do I stop retries from writing the same record twice?", "Keep every side effect outside the retry loop. The model call and validation belong inside; the database write happens once after the loop returns, keyed by an idempotency key you derive deterministically, such as a hash of the source document plus the extraction version. Never let the model generate the key — it will produce a different one on retry."),
  ("Does temperature 0 make structured output deterministic?", "No. Temperature 0 makes sampling greedy, which reduces variation, but it does not guarantee byte-identical outputs across runs — batching, hardware, and kernel differences all introduce nondeterminism. Design for the possibility that the same input produces a different valid object, and validate every response rather than assuming a cached earlier result still applies."),
  ("How can I design a schema that reduces validation failures?", "Give every enum an explicit unknown or other value so the model is not forced to guess, put a reasoning string field before the answer field so it can work through the problem in-output, write a description on every field, keep the structure flat, and mark genuinely optional fields optional so the model is never asked to fabricate a value it cannot find."),
  ("Should I ever ask for structured output from a free-text task?", "No. Structured output exists to serve machine consumers. If the result is prose a human reads, wrapping it in JSON adds token overhead and a failure mode with no corresponding benefit. Reach for a schema when something downstream needs to branch on a field, not by default."),
 ],
 "faq_cards": [
  ("JSON mode vs tool calling?", "JSON mode only guarantees valid JSON — not your fields or types. Tool calling shapes the output to a JSON Schema you define, and models are trained heavily on emitting well-formed calls."),
  ("What is constrained decoding?", "Token-level masking so only schema-valid tokens can be sampled — invalid output becomes unrepresentable, not just discouraged. Ollama's format field, llama.cpp GBNF, Outlines, XGrammar."),
  ("Still need validation?", "Yes. Schemas guarantee shape, not semantics — they can't check a date is in the past or an ID exists. Validation is also where you catch failure and give the model one corrective retry."),
  ("Handling a truncated response?", "Check the stop reason before the parse. Hitting the token limit is a config failure; retrying identically truncates identically. Raise the limit, request less, or batch it."),
  ("Stopping duplicate writes?", "Keep side effects out of the retry loop. Write once after it returns, keyed by an idempotency key you derive from the source — never one the model generates."),
  ("Is temperature 0 deterministic?", "No. It makes sampling greedy but does not guarantee byte-identical output — batching and hardware introduce nondeterminism. Validate every response rather than assuming."),
  ("Schema tips that cut failures?", "An unknown value on every enum, a reasoning field before the answer, descriptions on every field, flat over nested, optional fields marked optional so nothing gets fabricated."),
  ("Always use structured output?", "No — only when a machine consumes it. For prose a human reads, a schema adds token overhead and a failure mode with no benefit."),
 ],
},

{
 "slug": "moe-model-macbook",
 "title": "Running a 30B MoE Model on a MacBook",
 "desc": "What mixture-of-experts changes about RAM, quantization levels, KV cache math, Ollama tuning, and realistic tokens per second on Apple Silicon.",
 "og_title": "Running a 30B MoE Model on a MacBook",
 "og_desc": "Memory like a 30B, speed like a 3B. Quantization, KV cache math, Ollama flags that matter, and when local is genuinely good enough.",
 "headline": "Running a 30B Mixture-of-Experts Model on a MacBook: Memory, Quantization, Context, and Real Throughput",
 "crumb": "30B MoE on a MacBook",
 "about": ["Local language models", "Mixture of experts", "Apple Silicon", "Model quantization"],
 "eyebrow": "Guide · AI engineering",
 "h1": "Memory like a 30B, speed like a 3B",
 "lead": "A mixture-of-experts model breaks the usual rule that bigger means slower. A 30B MoE with 3B active parameters needs the RAM of a 30B model and generates at roughly the speed of a 3B one. That trade is why a laptop can run something genuinely useful. Here is the memory arithmetic, the quantization tradeoffs, the Ollama settings that actually matter, and an honest account of when the local model is good enough and when it is not.",
 "level": "Intermediate",
 "nav": NAV_DEV,
 "body": [
  ("h2", "What mixture-of-experts actually changes"),
  ("p", "A dense transformer runs every parameter for every token. A 30B dense model at 4-bit quantization has to read roughly 17 GB of weights out of memory to produce one token, and memory bandwidth — not compute — is what limits generation on a laptop. That is why dense 30B models feel slow on Apple Silicon regardless of how fast the chip is."),
  ("p", "A mixture-of-experts model replaces the feed-forward block in each layer with a set of parallel \"experts\" and a small router that picks a handful of them per token. Qwen3-30B-A3B, the model I run daily, has about 30.5B total parameters and activates roughly 3B of them for any given token. Different tokens route to different experts, so all 30B must be resident and available — but only ~3B are read per token."),
  ("formula", "Dense 30B @ Q4:   ~17 GB resident, ~17 GB read per token\nMoE 30B-A3B @ Q4: ~17 GB resident, ~2 GB read per token\n\nRAM footprint:  set by TOTAL parameters\nGeneration speed: set by ACTIVE parameters"),
  ("p", "So the headline: you pay 30B prices in RAM and get something much closer to 3B speeds in generation. That is the entire reason a 30B-class model is usable on a laptop at all, and it is why \"how many parameters\" stopped being a single useful number."),
  ("callout", "The routing has a cost the arithmetic hides. Which experts a token needs is only known at generation time, and consecutive tokens often need different ones, so memory access is scattered rather than sequential. Real throughput lands well below the bandwidth ceiling the numbers above suggest — expect the ratio to help enormously and not to be the 8x the raw parameter math implies.")
  ,
  ("h2", "Unified memory: the Apple Silicon quirk"),
  ("p", "On Apple Silicon, CPU and GPU share one pool of memory. There is no separate VRAM to fill and no PCIe transfer to pay for, which is why a 64 GB MacBook can hold a model that would need a serious discrete GPU elsewhere. The catch is that macOS caps how much of that pool the GPU may wire down — around 75% by default — and everything else on your machine competes for the rest."),
  ("formula", "# see the current GPU wired-memory limit (0 = system default, ~75%)\nsysctl iogpu.wired_limit_mb\n\n# raise it — 56 GB on a 64 GB machine. Resets on reboot.\nsudo sysctl iogpu.wired_limit_mb=57344"),
  ("p", "Raise this only if you have measured a model spilling, and leave real headroom. Push it too high and the system starts swapping under memory pressure, which is dramatically worse than a slightly smaller model — you go from tokens per second to seconds per token."),
  ("h2", "Quantization: what each level costs you"),
  ("p", "Quantization stores weights at reduced precision. The size arithmetic is straightforward: total parameters times bits-per-weight divided by 8. For a 30.5B-parameter model:"),
  ("table", (["Level", "Approx. bits/weight", "Size for 30.5B params", "What you give up"],
             [["Q8_0", "~8.5", "~32 GB", "Nothing measurable. Also the least practical on a laptop."],
              ["Q6_K", "~6.6", "~25 GB", "Effectively nothing for chat and drafting."],
              ["Q5_K_M", "~5.5", "~21 GB", "Very little. Slight softening on exact recall and long code."],
              ["Q4_K_M", "~4.5", "~17 GB", "The standard sweet spot. Noticeable only on hard reasoning and precise code."],
              ["Q3_K_M", "~3.5", "~13 GB", "Real degradation. Instruction following and formatting break before knowledge does."],
              ["Q2_K", "~2.6", "~10 GB", "Do not. It will produce fluent text and reliably get things wrong."]])),
  ("p", "The published Q4_K_M build of Qwen3-30B-A3B lands around 18 GB on disk, close to the arithmetic. Q4_K_M is where almost everyone should be: it fits a 32 GB machine with room for context, and the quality gap to Q8 is small enough that most people cannot pick the outputs apart blind."),
  ("p", "The failure mode below Q4 is worth knowing because it is counterintuitive. Aggressive quantization does not primarily erase facts — it erases <em>precision</em>. The model still knows things and gets steadily worse at following formatting instructions, closing brackets, staying in the requested structure, and holding a constraint across a long answer. If a local model suddenly cannot keep to a JSON shape it handled last week, check the quantization level before you rewrite the prompt."),
  ("h2", "Context length is a second, separate RAM bill"),
  ("p", "Model weights are the fixed cost. The KV cache is the variable one, and it is the number people forget: every token in the context window stores a key and a value vector in every layer, for the whole session."),
  ("formula", "bytes/token = 2 × n_layers × n_kv_heads × head_dim × bytes_per_element\n\nQwen3-30B-A3B: 48 layers, 4 KV heads, head_dim 128, fp16 (2 bytes)\n= 2 × 48 × 4 × 128 × 2 = 98,304 bytes = 96 KiB per token\n\n  8,192 tokens ×  96 KiB = 0.75 GiB\n 40,960 tokens ×  96 KiB = 3.75 GiB\n131,072 tokens ×  96 KiB =  12  GiB"),
  ("p", "Grouped-query attention is doing a lot of work in that formula — only 4 KV heads instead of 32 cuts the cache by 8x versus full multi-head attention. Check your model's own config for these values rather than reusing mine; a model with more KV heads has a dramatically more expensive cache and the difference shows up as an out-of-memory error at exactly the context length you needed."),
  ("p", "You can halve the cache by quantizing it. In Ollama that is <code>OLLAMA_KV_CACHE_TYPE=q8_0</code>, which requires flash attention to be enabled. The quality cost of an 8-bit KV cache is very small; <code>q4_0</code> halves it again and does start to show, particularly on long-context recall."),
  ("h2", "Ollama settings that actually matter"),
  ("p", "Most Ollama tuning advice is noise. These five are not:"),
  ("table", (["Setting", "Value", "Why"],
             [["<code>OLLAMA_FLASH_ATTENTION</code>", "<code>1</code>", "Faster attention, less memory. Also a prerequisite for KV cache quantization."],
              ["<code>OLLAMA_KV_CACHE_TYPE</code>", "<code>q8_0</code>", "Halves KV cache memory at negligible quality cost. Needs flash attention on."],
              ["<code>OLLAMA_KEEP_ALIVE</code>", "<code>30m</code> or <code>-1</code>", "Stops the model unloading between requests. A cold load of an 18 GB model is tens of seconds you pay on every call."],
              ["<code>OLLAMA_MAX_LOADED_MODELS</code>", "<code>1</code>", "On a laptop, two resident 18 GB models means neither fits. Force one."],
              ["<code>num_ctx</code> (per request)", "the smallest that fits your task", "Context you configure is context you pay for in RAM whether or not you fill it."]])),
  ("warn", "Set these where they survive a reboot. On macOS, <code>launchctl setenv</code> does not persist, and neither does exporting them in a shell that Ollama was not launched from — the daemon reads its environment at start. Put them in the <code>EnvironmentVariables</code> block of a LaunchAgent plist that starts the server, and then actually verify with <code>ps eww $(pgrep ollama) | tr ' ' '\\n' | grep OLLAMA</code>. I lost a week to tuning flags that were never applied because the service was being started by a GUI app that never saw them.")
  ,
  ("h2", "Realistic tokens per second on Apple Silicon"),
  ("p", "Generation speed on a laptop is bounded by memory bandwidth, so the chip tier matters more than the core count. Roughly: base M-series chips land near 100–120 GB/s, Pro chips in the 150–275 GB/s range, and Max chips at 400 GB/s and up. Divide bandwidth by bytes-read-per-token for a theoretical ceiling, then expect real numbers well below it."),
  ("p", "On my M1 Max (400 GB/s) running Qwen3-30B-A3B at Q4_K_M, generation sits in the tens of tokens per second at short context and degrades as the context fills — by 20k tokens in, it is meaningfully slower, because attention cost grows with sequence length even though the weight reads do not. Do not trust anyone's numbers including mine; measure your own, because quantization, context length, and what else is running all move it substantially."),
  ("formula", "curl -s http://localhost:11434/api/chat -d '{\n  \"model\": \"qwen3:30b-a3b\", \"stream\": false,\n  \"messages\": [{\"role\":\"user\",\"content\":\"Write 300 words about memory bandwidth.\"}]\n}' | python3 -c '\nimport sys, json\nd = json.load(sys.stdin)\nprint(\"prefill tok/s:\", round(d[\"prompt_eval_count\"] / (d[\"prompt_eval_duration\"]/1e9), 1))\nprint(\"gen     tok/s:\", round(d[\"eval_count\"] / (d[\"eval_duration\"]/1e9), 1))'"),
  ("h3", "The number that actually decides your architecture: prefill"),
  ("p", "Generation speed is what people benchmark. Prompt processing — prefill — is what determines whether your application feels usable, and it is a completely different regime: compute-bound rather than bandwidth-bound, and on Apple Silicon it runs at a few hundred tokens per second rather than the thousands a datacenter GPU manages."),
  ("p", "Work the arithmetic for a retrieval pipeline. A <a href=\"/guides/rag-explained.html\">RAG</a> query with 20,000 tokens of retrieved context, prefilling at 400 tokens/sec, spends 50 seconds before the first word of the answer appears. The generation phase afterwards might take four seconds. Your users experience a 54-second response and the tokens-per-second number you optimized was irrelevant to 90% of it."),
  ("p", "That single fact should shape the design. Retrieve fewer, better chunks — which is what <a href=\"/guides/embedding-model-rag.html\">choosing an embedding model and a reranker properly</a> buys you. Keep the stable part of your prompt stable so the runtime can reuse its cached KV state rather than reprefilling it, the local analogue of <a href=\"/guides/prompt-caching.html\">prompt caching on a hosted API</a>. And prefer batch work over interactive work wherever the task allows."),
  ("h2", "When local is genuinely good enough"),
  ("p", "The honest split is about latency tolerance and correctness stakes, not about capability in the abstract."),
  ("table", (["Local wins", "Pay for an API"],
             [["Batch jobs and overnight runs — nobody is waiting.", "Anything interactive where a user watches a cursor blink."],
              ["High-volume, low-stakes work: classification, tagging, first-draft generation.", "Long-context reasoning over big documents — prefill alone rules it out."],
              ["Anything where the data cannot leave the machine. This argument beats every quality argument.", "Code generation where correctness matters and a subtle bug costs hours."],
              ["Iterating on prompts, where free retries change how you work.", "Reliable multi-step tool calling. Frontier models are meaningfully better at it."],
              ["Fixed monthly cost matters more than best-possible output.", "Deadlines. Local throughput is not something you can scale up at 2am."]])),
  ("p", "My own split is exactly this. Overnight automation drafts on the local model — free retries, no rate limits, and if a draft is bad the cost of regenerating it is zero. Anything with a deadline or a correctness requirement goes to a hosted API, where the current Claude lineup runs from <code>claude-haiku-4-5-20251001</code> for cheap high-volume work through <code>claude-sonnet-5</code> and <code>claude-opus-5</code> for the hard problems. The two are not competitors; they are different tools that happen to share an interface."),
  ("h2", "A working setup, start to finish"),
  ("ol", [
   "Check your RAM. 32 GB runs a 30B MoE at Q4_K_M with modest context. 64 GB runs it comfortably with room for a large window. 16 GB should be running a 7B-class model instead — this guide is not for you and you will be happier.",
   "Pull the Q4_K_M build. Start there always; move up only if you can demonstrate a quality problem, and move down only if you cannot fit.",
   "Set the five environment variables above in a LaunchAgent plist so they survive a reboot, then verify with <code>ps eww</code> that the running process actually has them.",
   "Set <code>num_ctx</code> per request to the smallest value your task needs. Do not configure 128k because it is available.",
   "Measure prefill and generation separately with the timing script above, at the context length you actually use.",
   "Design around prefill, not generation. Fewer retrieved chunks, stable prompt prefixes, batch where you can.",
   "Keep an API path for the work that needs it. A local-only architecture is a constraint, not a virtue.",
  ]),
  ("p", "The setup takes an afternoon and then largely maintains itself. Everything I have built on top of it — the automation drafting, the classification passes, the overnight batch work you can see the shape of on <a href=\"/projects.html\">the projects page</a> — runs on that one resident model, and the whole thing costs the electricity to keep a laptop awake."),
 ],
 "tools": [("/guides/local-llm-ollama.html", "Local LLM with Ollama", "the setup layer beneath this guide — model selection, the HTTP API, and quantisation basics."),
           ("/guides/prompt-caching.html", "Prompt caching", "why keeping the stable part of a prompt stable matters locally and on a hosted API."),
           ("/guides/embedding-model-rag.html", "Embedding models for RAG", "retrieving fewer, better chunks is the main defence against slow prefill."),
           ("/stack.html", "My stack", "the actual hardware and tooling behind this.")],
 "faqs": [
  ("How much RAM do I need to run a 30B MoE model?", "Around 32 GB is the practical floor for a 30B-parameter mixture-of-experts model at Q4_K_M quantization, which needs roughly 17 to 18 GB for weights plus a few gigabytes for the KV cache and headroom for the rest of the system. 64 GB runs it comfortably with a large context window. Note that RAM is set by total parameters, not active ones, so the MoE architecture does not reduce the memory requirement at all."),
  ("Why is a 30B MoE model faster than a 30B dense model?", "Generation speed on a laptop is limited by memory bandwidth, and a mixture-of-experts model only reads the parameters of the few experts its router selects for each token, roughly 3B out of 30B. A dense model reads all 30B for every token. Both models occupy the same RAM, but the MoE reads far less per token, so it generates much faster."),
  ("Which quantization level should I use?", "Q4_K_M for almost everyone. It is the standard sweet spot: roughly 4.5 bits per weight, small enough to fit a 32 GB machine with context to spare, and close enough to Q8 that most people cannot distinguish the outputs blind. Go to Q5_K_M if you have the memory and do precision-sensitive work; avoid anything below Q4, where instruction-following and formatting degrade before factual knowledge does."),
  ("How much memory does the context window use?", "Compute it as 2 times layers times KV heads times head dimension times bytes per element, per token. For a model with 48 layers, 4 KV heads and head dimension 128 at fp16, that is 96 KiB per token — so 40,000 tokens of context costs about 3.75 GiB on top of the weights. Setting the KV cache type to q8_0 in Ollama roughly halves it at negligible quality cost."),
  ("What Ollama settings actually improve performance?", "Five: OLLAMA_FLASH_ATTENTION set to 1, OLLAMA_KV_CACHE_TYPE set to q8_0, OLLAMA_KEEP_ALIVE at 30m or -1 so the model does not unload between calls, OLLAMA_MAX_LOADED_MODELS set to 1 on a laptop, and num_ctx set per request to the smallest value the task needs. Everything else is noise by comparison."),
  ("Why do my Ollama environment variables not seem to apply?", "The daemon reads its environment when it starts, so variables exported in an unrelated shell never reach it, and on macOS launchctl setenv does not survive a reboot. Bake them into the EnvironmentVariables block of the LaunchAgent plist that starts the server, then verify against the live process with ps eww on the Ollama PID rather than trusting that they took."),
  ("What tokens per second should I expect on Apple Silicon?", "It scales with memory bandwidth, so roughly 100 to 120 GB/s on base chips, 150 to 275 on Pro, and 400 or more on Max. A 30B MoE at Q4 on a 400 GB/s machine generates in the tens of tokens per second at short context and slows as context fills. Measure your own with the eval_count and eval_duration fields the Ollama API returns rather than trusting any published figure."),
  ("When should I pay for an API instead of running locally?", "When latency is user-visible, when the prompt is long enough that prefill dominates, when correctness genuinely matters, or when you need reliable multi-step tool calling. Local wins on batch work, high-volume low-stakes tasks, prompt iteration, and anything where the data must not leave your machine — which is an argument that outweighs any quality comparison."),
 ],
 "faq_cards": [
  ("RAM for a 30B MoE?", "About 32 GB minimum at Q4_K_M — roughly 17-18 GB weights plus KV cache and headroom; 64 GB is comfortable. RAM is set by total parameters, so MoE saves you nothing here."),
  ("Why is MoE faster?", "Generation is memory-bandwidth bound, and a MoE reads only its routed experts — about 3B of 30B — per token. Same RAM as a dense 30B, a fraction of the reads."),
  ("Which quantization?", "Q4_K_M for almost everyone: fits 32 GB with context to spare and is hard to distinguish from Q8 blind. Below Q4, formatting and instruction-following break before knowledge does."),
  ("Context memory cost?", "2 × layers × KV heads × head_dim × bytes, per token. At 48 layers / 4 KV heads / 128 dim / fp16 that's 96 KiB per token — 3.75 GiB at 40k. q8_0 KV cache halves it."),
  ("Ollama settings that matter?", "Flash attention on, KV cache q8_0, keep-alive 30m or -1, max loaded models 1, and num_ctx set to the smallest value the task needs. The rest is noise."),
  ("Env vars not applying?", "The daemon reads its environment at start, and launchctl setenv doesn't survive reboot. Bake them into the LaunchAgent plist, then verify with ps eww on the live PID."),
  ("Tokens per second?", "Scales with bandwidth: ~100-120 GB/s base, 150-275 Pro, 400+ Max. A 30B MoE at Q4 on 400 GB/s runs in the tens of tok/s and slows as context fills. Measure your own."),
  ("When to pay for an API?", "User-visible latency, long prompts where prefill dominates, correctness-critical code, reliable tool calling. Local wins on batch, volume, iteration, and data that can't leave."),
 ],
},

{
 "slug": "prompt-caching",
 "title": "Prompt Caching and What It Actually Saves",
 "desc": "How LLM prefix caching works, cache TTL and break-even math, ordering a prompt so the stable part comes first, and when caching costs you more.",
 "og_title": "Prompt Caching and What It Actually Saves",
 "og_desc": "Prefix matching, TTL windows, the break-even arithmetic, and the traffic pattern where caching makes your bill 24% worse.",
 "headline": "Prompt Caching and What It Actually Saves: Prefix Matching, TTL, and the Break-Even Math",
 "crumb": "Prompt Caching",
 "about": ["Large language models", "API cost optimization", "Latency optimization", "System design"],
 "eyebrow": "Guide · AI engineering",
 "h1": "Prompt caching, and the traffic pattern where it costs you more",
 "lead": "Prompt caching can cut the input cost of a repeated large prompt by around 87% and remove most of the time-to-first-token. It can also make your bill 24% worse, and the difference is entirely down to how often your requests arrive and what order your prompt is assembled in. Here is how prefix caching works, the arithmetic that decides whether it pays, and the mistakes that silently produce a 0% hit rate.",
 "level": "Intermediate",
 "nav": NAV_DEV,
 "body": [
  ("h2", "What is actually being cached"),
  ("p", "Before a model generates its first output token it must process every input token — the prefill phase. That work produces a large intermediate structure, the key-value cache, holding one key and one value vector per token per layer. For a 20,000-token prompt that is a substantial amount of computation and it is identical every time the prompt is identical."),
  ("p", "Prompt caching stores that intermediate state on the provider's side and reuses it when a later request begins with the same tokens. You are not caching the response — same prompt, different sampling, still a different answer. You are caching the prefill work, which means you get both the cost reduction and the latency reduction, and the latency reduction is often the part users actually notice."),
  ("h2", "It is a prefix match, and that is the whole design"),
  ("p", "This is the one rule everything else follows from: <strong>the match starts at token zero and runs forward until the first difference.</strong> Everything after that difference is a miss. It is not a substring search, not a fuzzy match, and not per-block."),
  ("formula", "Request A: [system 4,000 tok][docs 16,000 tok][question 500 tok]\nRequest B: [system 4,000 tok][docs 16,000 tok][different question]\n           └──────── 20,000 tokens shared ────────┘  ← cache hit\n\nRequest C: [\"Today is 2026-07-31.\"][system][docs][question]\n           └── one changed token at position 0 ──┘  ← 0% hit rate"),
  ("p", "Request C is the mistake that destroys more caching implementations than everything else combined. A timestamp, a request UUID, or a user's name interpolated into the top of a system prompt changes byte zero of the prefix on every single call, and nothing after it can ever be reused. The prompt looks 99.9% identical. The cache hit rate is exactly zero."),
  ("p", "The corollary is an ordering rule you can apply mechanically: assemble the prompt in descending order of stability. Tool definitions and the frozen system prompt first, then static documents and few-shot examples, then conversation history, then the current user turn. Anything that varies per request goes last, always."),
  ("h2", "TTL: the window you have to hit"),
  ("p", "Cache entries expire. The standard window is five minutes from last use — each hit refreshes it — and providers typically also offer a longer one-hour option at a higher write cost. Five minutes is short enough that the traffic pattern of your application, not the size of your prompt, is what decides whether caching helps."),
  ("p", "Two applications with identical prompts can land on opposite sides of this. A chat interface where a user sends a follow-up every thirty seconds hits the cache on every turn after the first. A weekly report generator that fires one request at a time, ten minutes apart, misses on every single one and pays the write premium each time for a cache nothing ever reads."),
  ("h2", "The cost arithmetic"),
  ("p", "Caching is not free to populate. Writing an entry costs more than a normal input token, and reading one costs dramatically less. The multipliers, relative to the base input price:"),
  ("table", (["Operation", "Cost vs. base input", "When it happens"],
             [["Cache write, 5-minute TTL", "1.25×", "First request with a new prefix, or any request after expiry."],
              ["Cache write, 1-hour TTL", "2×", "Same, with the longer window."],
              ["Cache read", "~0.1×", "Every subsequent request that matches the prefix within the TTL."],
              ["Uncached input", "1×", "Everything after the last cached breakpoint."],
              ["Output tokens", "1× (unaffected)", "Always. Caching does nothing for output cost, ever."]])),
  ("p", "From those two multipliers you can derive the break-even directly. For N requests sharing one prefix within the TTL window, with W tokens in the shared prefix:"),
  ("formula", "cached cost   = 1.25·W  +  0.1·W·(N−1)\nuncached cost = N·W\n\nset equal:  1.25 + 0.1(N−1) = N\n            1.15 = 0.9N\n               N = 1.28\n\n5-minute TTL: caching pays from the SECOND request onward.\n1-hour TTL (2× write): break-even at N = 2.11 — you need a THIRD."),
  ("p", "That is a low bar and it is why caching is close to free money in the common case. The trap is that N counts requests <em>within one TTL window</em>, not requests in total. Two hundred requests spread evenly across a day is N = 1 for every window."),
  ("h2", "A worked example, both directions"),
  ("p", "Take a support assistant: a 20,000-token stable prefix (system instructions, tool definitions, a product knowledge base) plus a 500-token variable user question. Two hundred requests in an hour."),
  ("formula", "BUSY HOUR — 200 requests, ~18 seconds apart, all inside the 5-min TTL\n\nNo caching:   200 × 20,500                    = 4,100,000 effective tokens\nWith caching:   1 × 20,000 × 1.25 =    25,000\n               199 × 20,000 × 0.1 =   398,000\n               200 ×    500 × 1.0 =   100,000\n                                     ─────────\n                                       523,000 effective tokens\n\nReduction: 87.2%.\nAt a $3-per-million input rate: $12.30 -> $1.57 per hour."),
  ("p", "Now the same 200 requests, same prompt, spread evenly across a 24-hour day — one every 7.2 minutes, which is longer than the five-minute TTL. Every request finds an expired entry, writes a fresh one, and nothing ever reads it:"),
  ("formula", "SPARSE DAY — 200 requests, 7.2 minutes apart, every one a cold write\n\nNo caching:   200 × 20,500              = 4,100,000 effective tokens\nWith caching: 200 × 20,000 × 1.25       = 5,000,000\n              200 ×    500 × 1.0        =   100,000\n                                          ─────────\n                                          5,100,000 effective tokens\n\n24% MORE EXPENSIVE than not caching at all."),
  ("warn", "Same prompt, same request count, same code — the only variable was arrival timing, and it moved the result from an 87% saving to a 24% penalty. This is why \"turn on caching everywhere\" is bad advice and why the hit rate is the only number that tells you which regime you are in.")
  ,
  ("h2", "When caching does nothing, or worse"),
  ("ul", [
   "<strong>The prefix is below the minimum cacheable length.</strong> Providers enforce a floor, commonly in the 512 to 4,096 token range depending on the model, and it is not monotonic across model generations. Below it, the cache marker is silently ignored — no error, no entry, and a diagnostic field showing zero tokens written.",
   "<strong>Something varies at the front.</strong> A timestamp, a UUID, a session ID, a user's name in the system prompt. Byte zero differs, everything after it misses.",
   "<strong>Traffic is sparser than the TTL.</strong> The case above. Either batch requests together in time or switch to the longer TTL and accept the higher write cost.",
   "<strong>Tool definitions changed.</strong> Tools usually render at the very front of the prompt, so adding, removing, or reordering one invalidates everything. Serialise them deterministically — sort by name — because an unordered dict or set will reorder itself between runs and you will never see why.",
   "<strong>Non-deterministic JSON serialisation.</strong> <code>json.dumps</code> without <code>sort_keys=True</code>, or anything iterating a set, produces a byte-different prefix from identical data.",
   "<strong>You switched models.</strong> Caches are model-scoped. Routing between models mid-conversation means each one maintains its own entry and neither is warm.",
   "<strong>Concurrent identical requests.</strong> An entry only becomes readable once the first response begins streaming. Fire ten parallel requests with the same prefix and all ten pay the write. Send one, wait for its first token, then fan out the rest.",
   "<strong>You expected it to reduce output cost.</strong> It does not. Output tokens are billed at full price regardless, and on a short-prompt, long-answer workload caching is nearly pointless.",
  ]),
  ("h2", "Measure it or you do not know"),
  ("p", "Every provider that offers caching reports it in the response usage object — typically a count of tokens written to cache, a count read from cache, and a count processed uncached. Log all three on every call and compute a hit rate. This is not optional instrumentation; a caching implementation you are not measuring is one you cannot distinguish from a broken one, and the broken version costs 25% more while looking identical in your code."),
  ("formula", "hit_rate = cache_read_tokens / (cache_read_tokens + cache_write_tokens + uncached_tokens)\n\n> 0.8   working as intended\n0.3-0.8 partial — check whether traffic is sparser than the TTL\n~ 0.0   something varies in the prefix, or it is below the minimum length\n\n# total prompt size = uncached + cache_creation + cache_read\n# an agent showing 4k \"input tokens\" after hours of work is not small —\n# the rest was served from cache. Sum the three, never read one."),
  ("p", "If the hit rate is zero across repeated calls you believe are identical, do not theorise — diff the rendered prompts. Serialise the exact bytes sent on two consecutive requests to files and run a diff. The culprit is always visible and it is always something small and boring."),
  ("h2", "Architectural moves that raise the hit rate"),
  ("ul", [
   "<strong>Freeze the system prompt.</strong> Never interpolate the date, the user, or a mode flag into it. Inject that context later in the message list, where invalidating from that point costs almost nothing.",
   "<strong>Batch by prefix.</strong> If you have a queue of jobs sharing a large prefix, sort the queue by prefix and run each group together. This is the single change that converts the sparse-traffic penalty into the busy-hour saving.",
   "<strong>Do not swap tools mid-conversation.</strong> Tools sit at position zero. If you need modes, pass the mode as message content rather than changing which tools exist.",
   "<strong>Make forked calls reuse the parent prefix exactly.</strong> Summarisation passes, sub-agents, and side computations often rebuild the system prompt from scratch and miss the cache they were sitting on. Copy the parent's prefix verbatim and append.",
   "<strong>Pre-warm only when the first request's latency is user-visible.</strong> A startup warm-up call is a real write charge; it is worth it for an interactive surface after a deploy and it is waste for a background job.",
   "<strong>Consider the longer TTL for bursty traffic.</strong> Gaps longer than five minutes but shorter than an hour are exactly the case the one-hour window exists for — at 2× write cost, so you need three hits per window instead of two.",
  ]),
  ("h2", "The local equivalent"),
  ("p", "Local runtimes do the same thing under a different name. llama.cpp and Ollama keep the KV cache in memory between requests and reuse the matching prefix automatically, which is why a follow-up question in the same session starts generating almost immediately while a fresh session with the same context takes seconds. The mechanism is identical; the economics are not, because locally you are saving wall-clock time rather than money."),
  ("p", "That makes the ordering discipline more important locally, not less, since <a href=\"/guides/moe-model-macbook.html\">prefill on Apple Silicon runs at a few hundred tokens per second</a> and a 20,000-token prompt reprocessed from scratch is a genuinely long wait. Same rule, same reason: stable content first, volatile content last. Between that and retrieving fewer, better chunks — which is what <a href=\"/guides/embedding-model-rag.html\">a properly evaluated retrieval pipeline</a> gives you — you can usually take a local RAG setup from unusable to pleasant without touching the model at all."),
  ("h2", "The short version"),
  ("ol", [
   "Caching stores prefill work, keyed on an exact prefix match from token zero.",
   "Order the prompt stable-first: tools, system, static documents, history, then the current turn.",
   "Writes cost about 1.25× base input on the short TTL; reads cost about 0.1×.",
   "It pays from the second hit within the TTL window, and it costs you 24% more if you never get one.",
   "Log the three usage counters and track a hit rate. Zero means something varies in your prefix.",
   "It never reduces output cost. If your prompts are short and your answers long, this is not your lever.",
  ]),
 ],
 "tools": [("/guides/moe-model-macbook.html", "30B MoE on a MacBook", "why prefix reuse matters even more locally, where prefill is the bottleneck."),
           ("/guides/embedding-model-rag.html", "Embedding models for RAG", "retrieving fewer chunks shrinks the prompt caching has to work on."),
           ("/guides/structured-llm-output.html", "Structured output without regret", "the other half of making LLM calls cheap and reliable in production."),
           ("/services.html", "Services", "automation work built on exactly these cost and latency tradeoffs.")],
 "faqs": [
  ("How does prompt caching actually work?", "The provider stores the key-value state produced while processing your prompt's input tokens, then reuses it when a later request begins with the identical token sequence. You are caching the prefill computation, not the response, so you save both input cost and time-to-first-token while still getting a freshly generated answer each time."),
  ("Why is my prompt cache hit rate zero?", "Almost always because something varies at the front of the prompt. A timestamp, a request UUID, a session ID, or a user's name interpolated into the system prompt changes the prefix at position zero, and since matching runs forward from token zero until the first difference, nothing after it can be reused. Diff the exact bytes of two consecutive requests and the culprit is always visible."),
  ("How long does a prompt cache entry last?", "The standard window is about five minutes from last use, refreshed on each hit, with a longer one-hour option available at roughly double the write cost. Five minutes is short enough that your request arrival pattern, not your prompt size, determines whether caching helps at all."),
  ("How many requests do I need before prompt caching pays off?", "With the five-minute window, caching pays from the second matching request onward: a write costs about 1.25 times base input and a read about 0.1 times, so two requests cost 1.35 units versus 2 uncached. With the one-hour window the write costs about 2 times, pushing break-even to the third request. The critical detail is that these counts are per TTL window, not lifetime totals."),
  ("Can prompt caching ever cost more than not caching?", "Yes, and it is a common failure. If your requests arrive further apart than the TTL, every one writes a fresh entry that nothing ever reads, and you pay the write premium each time. Two hundred requests spaced 7.2 minutes apart against a five-minute TTL comes out roughly 24% more expensive than not caching at all."),
  ("Does prompt caching reduce output token cost?", "No. Caching only affects the input side by skipping prefill work on the matched prefix. Output tokens are billed at full price regardless of hit rate, so a workload with short prompts and long generated answers gets almost nothing from caching no matter how it is configured."),
  ("How should I order a prompt to maximize cache hits?", "Descending order of stability. Tool definitions and a frozen system prompt first, then static documents and few-shot examples, then conversation history, and the current user turn last. Anything that changes per request belongs at the very end, so that changing it invalidates as little of the prefix as possible."),
  ("Why do parallel requests with the same prompt all miss the cache?", "A cache entry only becomes readable once the first response starts streaming, so simultaneous requests are all writing rather than reading. Send one request, wait for its first token, then fan out the rest — they will read the entry the first one just wrote instead of each paying full price."),
 ],
 "faq_cards": [
  ("How does prompt caching work?", "It stores the key-value state from processing your input tokens and reuses it when a later request starts with the identical token sequence. You cache the prefill, not the answer."),
  ("Why is my hit rate zero?", "Something varies at the front. A timestamp, UUID, or username in the system prompt changes position zero, and matching runs forward from there. Diff two requests' exact bytes."),
  ("How long does a cache entry last?", "About five minutes from last use, refreshed on each hit, with a one-hour option at roughly double the write cost. Arrival timing, not prompt size, decides whether it helps."),
  ("How many requests to break even?", "Two, on the five-minute window: a 1.25x write plus a 0.1x read beats two full-price requests. Three on the one-hour window, where writes cost 2x. Counted per window, not lifetime."),
  ("Can caching cost more?", "Yes. Requests spaced further apart than the TTL write an entry nobody reads. 200 requests 7.2 minutes apart against a 5-minute TTL runs about 24% more expensive than no caching."),
  ("Does it cut output cost?", "No, never. Caching only skips prefill on the matched input prefix. Short prompts with long answers get almost nothing from it regardless of configuration."),
  ("Best prompt ordering?", "Descending stability: tools, frozen system prompt, static documents, few-shot examples, history, then the current turn. Anything per-request goes dead last."),
  ("Why do parallel calls all miss?", "An entry is only readable once the first response starts streaming, so simultaneous requests all write. Send one, wait for its first token, then fan out the rest."),
 ],
},

{
 "slug": "static-site-ai-search",
 "title": "A Static Site AI Search Engines Can Read",
 "desc": "llms.txt, robots.txt rules for named AI crawlers, JSON-LD entity graphs with @id, mirrored FAQ schema, and what an answer engine cannot see in an SPA.",
 "og_title": "A Static Site AI Search Engines Can Actually Read",
 "og_desc": "The build side of AI search: llms.txt, named AI user-agents, @id entity graphs, semantic HTML, and why a client-rendered SPA is invisible.",
 "headline": "Shipping a Static Site AI Search Engines Can Actually Read: llms.txt, Entity Graphs, and the SPA Problem",
 "crumb": "Static Sites for AI Search",
 "about": ["Search engine optimization", "Structured data", "Answer engine optimization", "Web development"],
 "eyebrow": "Guide · AI engineering",
 "h1": "What an answer engine actually fetches from your site",
 "lead": "Three different things now read your pages: a classic search crawler that eventually runs your JavaScript, a training crawler that mostly does not, and a live retrieval fetcher inside an answer engine that definitely does not and has a timeout. This is the build-side guide — the files, the markup, and the verification commands that decide what each of them can see.",
 "level": "Intermediate",
 "nav": NAV_DEV,
 "body": [
  ("h2", "Three consumers, three capabilities"),
  ("p", "The strategy question — how to get cited when the answer replaces the click — is covered in <a href=\"/guides/ai-search-optimization.html\">the AI search optimization guide</a>. This one is about the build: what you actually ship so that the strategy has something to work with. And that starts with being precise about who is fetching."),
  ("table", (["Consumer", "Runs your JavaScript?", "What it wants"],
             [["Classic search crawler", "Yes, on a delay and a budget.", "Rendered content, structured data, links, a sitemap."],
              ["AI training crawler", "Usually not.", "Raw HTML text, at scale, cheaply."],
              ["Live retrieval fetcher inside an answer engine", "No, and it has a hard timeout.", "A single fast HTML response it can extract an answer from right now."]])),
  ("p", "That third row is the one that changes how you build. When someone asks an assistant a question and it fetches your page to answer, there is no render queue and no second pass. It gets one HTTP response, extracts what it can, and moves on. Whatever is not in that response does not exist."),
  ("h2", "Test what a non-rendering fetcher sees"),
  ("p", "One command settles the question. Fetch your page with no JavaScript engine, strip the tags, and count the words:"),
  ("formula", "curl -sL https://yoursite.com/some-page \\\n  | python3 -c \"import sys,re,html;s=sys.stdin.read();\ns=re.sub(r'(?is)&lt;(script|style|noscript).*?&lt;/\\\\1&gt;',' ',s);\nprint(len(html.unescape(re.sub('&lt;[^&gt;]+&gt;',' ',s)).split()),'words')\"\n\n# A 2,000-word article should report roughly 2,000 words.\n# If it reports 12, everything a reader sees is being drawn by JavaScript."),
  ("p", "The failure looks like this in the source: <code>&lt;div id=\"root\"&gt;&lt;/div&gt;</code> and a script tag. That is the entire page as far as a non-rendering client is concerned. Every word of your content, every heading, every link exists only after a bundle downloads, parses, executes, and fetches data — none of which happens for an answer engine on a two-second budget."),
  ("p", "The fix is not clever, it is structural: <strong>generate real HTML files.</strong> Static site generation, server-side rendering with a real HTML response, or a prerender step in your build. All three produce the same thing that matters — a document with the content already in it. The page you are reading is a generated static file: a Python script holds the content as data, renders the markup and structured data, and writes an HTML file. There is no client-side hydration involved and there is nothing to miss."),
  ("h2", "Semantic HTML over div soup"),
  ("p", "Extractors — the readability-style algorithms that strip navigation and boilerplate to find the article — score elements on their tags and structure. Content wrapped in generic containers scores worse than the same content in semantic ones, and in a few cases the meaning is lost entirely rather than merely downgraded."),
  ("ul", [
   "<strong>One <code>&lt;h1&gt;</code>, then a heading hierarchy that matches the outline.</strong> Heading structure is how an extractor segments the page into topics, and it is how a retrieval system decides which section answers a question.",
   "<strong><code>&lt;main&gt;</code> and <code>&lt;article&gt;</code> around the real content.</strong> The cheapest possible signal for \"this is the page, the rest is furniture.\"",
   "<strong>Real <code>&lt;table&gt;</code> markup for tabular data, with <code>&lt;th&gt;</code>.</strong> A comparison table built from nested flex divs is not a table to anything but a browser — the row and column relationships are gone, and those relationships were the information.",
   "<strong>Real <code>&lt;ol&gt;</code> and <code>&lt;ul&gt;</code> for lists.</strong> A sequence of styled divs is a sequence of paragraphs to an extractor.",
   "<strong>Descriptive link text.</strong> \"Click here\" tells a machine nothing about the destination; the link text is a large part of how the target page's topic is inferred.",
   "<strong>Text in text, not in images.</strong> A chart rendered as a PNG with the numbers baked in is invisible. Put the numbers in a table next to it.",
  ]),
  ("h2", "llms.txt: cheap, useful, and not a standard"),
  ("p", "llms.txt is a proposed convention: a Markdown file at your domain root giving language models a curated, machine-readable index of your site. The format is an H1 with the site name, an optional blockquote summary, then H2 sections of links, each with a short description after a colon."),
  ("formula", "# Your Name\n\n> One-paragraph description of who you are and what this site contains.\n\n## Guides\n- [Choosing an embedding model for RAG](https://site.com/guides/embedding-model-rag.html): dimensions vs cost, recall@k evaluation, when a reranker wins.\n- [Prompt caching](https://site.com/guides/prompt-caching.html): prefix matching, TTL, break-even math.\n\n## Projects\n- [DIRA](https://site.com/dira.html): zero-dependency Python security scanner."),
  ("p", "A companion <code>llms-full.txt</code> holds the same material expanded — full descriptions rather than one-liners — for a consumer that wants everything in one fetch. Serve both at the root, and copy <code>llms.txt</code> to <code>/.well-known/llms.txt</code> as well, since that is where a second convention looks for it."),
  ("p", "Be honest about what this buys you: no major provider has publicly committed to consuming these files, and adoption is uneven. It is cheap insurance rather than a ranking lever. The reason to do it anyway is that it is genuinely useful independent of any crawler — it is a machine-readable index of your own site that you control, and it takes minutes to generate."),
  ("p", "The important part is generating it from the same source your pages come from. Two hand-maintained lists diverge within a month. On this site the guide registry is a Python data structure; the pages, the hub, the sitemap, and both llms files are all rendered from it, so a new guide cannot exist on one surface and be missing from another."),
  ("h2", "robots.txt and the named AI user-agents"),
  ("p", "AI companies operate several crawlers with distinct names and distinct purposes, and the single most useful thing to understand here is that they are not interchangeable. Blocking the wrong one removes you from citations while doing nothing about training."),
  ("table", (["User-agent", "Purpose", "Cost of blocking it"],
             [["<code>GPTBot</code>, <code>ClaudeBot</code>, <code>CCBot</code>, <code>Bytespider</code>", "Bulk crawling, largely for training corpora.", "None to your visibility. This is the one to block if you object to training use."],
              ["<code>ChatGPT-User</code>, <code>Claude-User</code>, <code>Perplexity-User</code>", "Fetches your page live because a user's question needs it right now.", "High. Block this and you cannot be cited in an answer."],
              ["<code>OAI-SearchBot</code>, <code>Claude-SearchBot</code>, <code>PerplexityBot</code>", "Builds the search index the assistant retrieves from.", "High. Block this and you are not in the index to retrieve."],
              ["<code>Google-Extended</code>, <code>Applebot-Extended</code>", "Training and grounding opt-outs only.", "None to search ranking — these do not affect Google Search or Apple search indexing at all."]])),
  ("p", "So a policy of \"opt out of training, stay visible in answers\" is expressible, and it looks like this:"),
  ("formula", "# opt out of bulk training crawls\nUser-agent: GPTBot\nDisallow: /\n\nUser-agent: CCBot\nDisallow: /\n\nUser-agent: Google-Extended\nDisallow: /\n\n# stay retrievable and citable\nUser-agent: OAI-SearchBot\nAllow: /\n\nUser-agent: Claude-User\nAllow: /\n\nUser-agent: PerplexityBot\nAllow: /\n\nSitemap: https://yoursite.com/sitemap.xml"),
  ("warn", "robots.txt is a request, not an access control. It is honoured by the major operators and ignored by anything that does not want to honour it. If content genuinely must not be fetched, it needs authentication, not a directive. Related: a stray <code>X-Robots-Tag: noindex</code> response header overrides everything in your robots.txt and your meta tags, silently — check it with <code>curl -sI</code> before debugging anything else.")
  ,
  ("h2", "JSON-LD entity graphs and @id linking"),
  ("p", "Structured data is how you state facts about a page in a form that needs no inference. The upgrade most sites have not made is going from a pile of disconnected snippets to a linked graph — every node carrying a stable <code>@id</code>, and other nodes referencing that ID instead of repeating the data."),
  ("formula", "{\n  \"@context\": \"https://schema.org\",\n  \"@graph\": [\n    {\n      \"@type\": \"Person\",\n      \"@id\": \"https://site.com/#person\",\n      \"name\": \"Your Name\",\n      \"url\": \"https://site.com/\",\n      \"sameAs\": [\"https://github.com/you\", \"https://linkedin.com/in/you\"]\n    },\n    {\n      \"@type\": \"TechArticle\",\n      \"@id\": \"https://site.com/guides/x.html#article\",\n      \"headline\": \"...\",\n      \"author\":    {\"@id\": \"https://site.com/#person\"},\n      \"publisher\": {\"@id\": \"https://site.com/#person\"}\n    }\n  ]\n}"),
  ("p", "Two things are doing work here. First, <code>author</code> is a reference rather than a duplicated object, so there is exactly one Person entity on the entire site and every page points at it — nothing to drift out of sync, and no ambiguity about whether two similar names are the same person. Second, <code>sameAs</code> is the reconciliation mechanism: it says \"the entity on this page is the same entity as these external profiles,\" which is precisely the link a system needs to merge your site's claims with what it already knows from elsewhere."),
  ("p", "Pick the type that matches what the page actually is — <code>TechArticle</code> for a technical guide, <code>SoftwareApplication</code> for a tool, <code>FAQPage</code> for questions and answers — and put a <code>BreadcrumbList</code> on anything nested, since that is how position in a hierarchy is communicated when the URL alone is ambiguous."),
  ("h2", "The FAQ rule that gets sites penalised"),
  ("p", "<strong>Every question and answer in your FAQPage schema must be visible on the page, in text, matching the markup.</strong> Not in a collapsed accordion that only populates on click. Not paraphrased. Not schema-only. Structured data that describes content a visitor cannot find is a guidelines violation and a manual-action risk, and it is a surprisingly common mistake because the schema is so easy to write in isolation."),
  ("p", "There is a second, more practical reason to mirror it. Plenty of extractors do not parse JSON-LD at all — they read the visible text. A question in schema and nowhere else is invisible to exactly the consumer you were trying to reach. Mirroring it means both paths find the same answer, which is the entire point."),
  ("p", "The reliable way to enforce this is to generate both from one source. On this site, each guide's FAQ entries are a list of question-answer pairs in a data file; the generator emits the FAQPage JSON-LD and the visible FAQ cards from that same list in the same pass. They cannot disagree, because there is no second copy to disagree with."),
  ("h2", "Write pages that survive being chunked"),
  ("p", "A retrieval system does not read your page, it reads a piece of it. Whatever the chunking strategy, some paragraph of yours will be evaluated on its own with no surrounding context — the same constraint that governs <a href=\"/guides/embedding-model-rag.html\">chunking a corpus for RAG</a>, applied to your own writing."),
  ("ul", [
   "<strong>Lead with the answer.</strong> Put the direct response in the first sentence under a heading, then the explanation. A chunk that opens with three sentences of setup is a chunk whose relevance is not apparent.",
   "<strong>Make headings question-shaped where it is natural.</strong> Heading text and query text living in the same space is a real retrieval advantage, not a trick.",
   "<strong>Keep paragraphs self-contained.</strong> \"As mentioned above\" is meaningless in a chunk. Repeat the noun instead of using a pronoun that refers three paragraphs back.",
   "<strong>Put numbers, dates, and units in the text.</strong> Specific figures are what get quoted; vague claims are not extractable.",
   "<strong>State scope explicitly.</strong> \"On Apple Silicon\" or \"for a five-minute cache window\" inside the sentence, not established once at the top of a section and assumed thereafter.",
  ]),
  ("h2", "What an answer engine cannot see, ever"),
  ("ul", [
   "Anything rendered by client-side JavaScript, including content fetched after page load.",
   "Content behind a cookie wall, an age gate, an email capture, or any interstitial.",
   "Text baked into images, canvas elements, or video, absent a transcript or alt text.",
   "Content that only appears after scrolling, if loading is genuinely deferred rather than merely hidden by CSS.",
   "Anything requiring authentication — obviously, and worth stating because staging environments get indexed and production ones get accidentally gated.",
   "Iframed content, generally, since the fetcher follows one URL and does not chase embeds.",
   "Anything your <code>robots.txt</code> disallows for that specific user-agent, which is why the training-versus-retrieval distinction above matters so much.",
  ]),
  ("h2", "A verification pass you can run in two minutes"),
  ("ol", [
   "<code>curl -sL https://site.com/page</code> and count the words after stripping tags. It should approximate the visible article length.",
   "<code>curl -sI https://site.com/page</code> and confirm the content type is <code>text/html</code> and there is no <code>X-Robots-Tag: noindex</code> hiding in the headers.",
   "Extract every <code>application/ld+json</code> block and run it through a JSON parser. One trailing comma silently invalidates the entire block and nothing warns you.",
   "Confirm every question in your FAQPage schema appears verbatim in the page text.",
   "Fetch <code>/robots.txt</code>, <code>/llms.txt</code>, <code>/.well-known/llms.txt</code>, and <code>/sitemap.xml</code> and confirm all four return 200 with the right content type.",
   "Diff your sitemap's URL list against the files you actually generated. A regeneration that silently drops a section is the classic failure, and it is invisible until traffic disappears.",
   "Confirm every internal link resolves to a file that exists. Dead internal links waste crawl budget and break the graph that connects your pages.",
  ]),
  ("p", "All seven are scriptable in about fifty lines, and running them as a pre-deploy gate is worth more than any individual optimisation, because the failure mode here is silent. Nothing errors when your sitemap loses fourteen URLs or a JSON-LD block stops parsing; the pages simply stop being found, and you notice months later. The same argument applies to the rest of what ships with a site — <a href=\"/guides/student-founder-security-checklist.html\">the security checklist</a> covers the automated gates that catch those failures, and <a href=\"/dira.html\">DIRA</a> runs the header and TLS half of this list against a live domain."),
  ("h2", "The part none of this fixes"),
  ("p", "Structured data, llms.txt, and semantic markup are parsing aids. They make it easy for a machine to understand what you wrote and impossible for it to misfile you. They do not make thin content worth citing. An answer engine cites a page because it contains a specific, checkable claim that answers a specific question — a number, a formula, a command, a tradeoff stated honestly. Everything in this guide exists to make sure that when you have written something worth citing, nothing in your build pipeline is standing in the way."),
 ],
 "tools": [("/guides/ai-search-optimization.html", "AI search optimization", "the strategy layer above this build guide — entity consolidation, answer-first writing, IndexNow."),
           ("/guides/embedding-model-rag.html", "Embedding models for RAG", "how retrieval chunks and ranks pages, which is why self-contained paragraphs matter."),
           ("/dira.html", "DIRA", "a free scanner that checks TLS, security headers, and what your live domain is exposing."),
           ("/apps.html", "Apps", "the same structured-data pattern applied to product pages.")],
 "faqs": [
  ("Can AI answer engines read a client-side rendered SPA?", "Usually not. A live retrieval fetcher answering a user's question makes one HTTP request with a hard timeout and no JavaScript engine, so a page whose body is an empty root div returns nothing usable. Test it with curl, strip the tags, and count the words — if a 2,000-word article reports a dozen words, your content is invisible to that consumer."),
  ("What is llms.txt and does it actually do anything?", "It is a proposed convention for a Markdown file at your domain root giving language models a curated index of your site: an H1 title, an optional blockquote summary, then H2 sections of links with short descriptions. No major provider has publicly committed to consuming it, so treat it as cheap insurance rather than a ranking lever — the real value is having one machine-readable index of your own site that you control and generate from the same source as your pages."),
  ("Should I block AI crawlers in robots.txt?", "Only the ones whose purpose you object to, because they are not interchangeable. Bulk training crawlers like GPTBot, ClaudeBot and CCBot can be blocked with no cost to visibility, but blocking retrieval agents such as ChatGPT-User, Claude-User or PerplexityBot removes you from the answers those assistants generate. Google-Extended and Applebot-Extended are training opt-outs only and do not affect search indexing at all."),
  ("What does @id do in JSON-LD?", "It gives a schema node a stable identifier that other nodes can reference instead of duplicating the data. Define one Person or Organization node with an @id like https://site.com/#person, then have every article's author and publisher fields point at that ID. You get exactly one canonical entity across the site with nothing to drift out of sync, and sameAs links reconcile it with your external profiles."),
  ("Does a visible FAQ have to match FAQPage schema?", "Yes. Structured data describing content a visitor cannot see on the page violates search guidelines and risks a manual action. There is also a practical reason: many extractors never parse JSON-LD and read only the visible text, so a schema-only question is invisible to the exact consumer you were targeting. Generate both from one source so they cannot diverge."),
  ("Does semantic HTML matter more than div soup for AI search?", "Yes, and in some cases the difference is total rather than marginal. Content extractors score elements by tag and structure, so a real table with th cells preserves row and column relationships that a nested flex-div layout destroys entirely. One h1, a heading hierarchy matching the outline, main and article wrappers, and real list elements are the highest-value markup changes available."),
  ("What can an answer engine never see on my page?", "Anything rendered by client-side JavaScript, content behind a cookie wall or email gate, text baked into images or canvas without alt text or a transcript, genuinely deferred lazy-loaded content, anything behind authentication, most iframed content, and anything your robots.txt disallows for that specific user-agent."),
  ("How do I verify my site is readable before deploying?", "Fetch each page with curl and count the words after stripping tags, check headers for a stray X-Robots-Tag noindex, parse every JSON-LD block to confirm it is valid, confirm each FAQ question appears verbatim in the visible text, fetch robots.txt and llms.txt and the sitemap, diff the sitemap's URL list against what you actually generated, and confirm every internal link resolves. All of it scripts in about fifty lines and belongs in a pre-deploy gate."),
 ],
 "faq_cards": [
  ("Can AI engines read an SPA?", "Usually not. A live retrieval fetch is one request, no JavaScript, hard timeout — an empty root div returns nothing. curl the page, strip tags, count words to check."),
  ("Does llms.txt do anything?", "No provider has publicly committed to consuming it, so treat it as cheap insurance. The real value is a machine-readable index of your own site, generated from the same source as your pages."),
  ("Block AI crawlers?", "Only the ones you object to. Blocking GPTBot/ClaudeBot/CCBot costs no visibility; blocking ChatGPT-User, Claude-User or PerplexityBot removes you from answers entirely."),
  ("What does @id do?", "It gives a schema node a stable ID other nodes reference instead of duplicating. One Person entity site-wide, referenced everywhere, reconciled to external profiles via sameAs."),
  ("Must the FAQ be visible?", "Yes. Schema describing invisible content violates guidelines and risks a manual action — and many extractors never parse JSON-LD, so schema-only questions reach nobody."),
  ("Semantic HTML vs div soup?", "It matters, sometimes totally. A real table with th preserves row/column relationships that nested flex divs destroy. One h1, real headings, main/article, real lists."),
  ("What can it never see?", "Client-rendered content, anything behind a cookie wall or auth, text baked into images, deferred lazy-loaded content, most iframes, and anything robots.txt disallows for that agent."),
  ("How do I verify before deploy?", "curl + word count, check headers for stray noindex, parse every JSON-LD block, confirm FAQ text is visible, fetch robots/llms/sitemap, diff the sitemap, check internal links."),
 ],
},

]
